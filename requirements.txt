# ===== ConveyorAI Confidence Score (Notebook) =====
# Create a fresh venv/conda env, then: pip install -r requirements.txt

# Core scientific stack
numpy>=1.24,<3
pandas>=2.0,<3
matplotlib>=3.7,<4
scikit-learn>=1.3,<2

# Jupyter for running the notebook
jupyter>=1.0.0

# OpenAI SDK (optional; only needed if you want live LLM calls)
openai>=1.40.0
